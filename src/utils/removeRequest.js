/*WHEN 5 DIFFERENT USERS FULFILL REQUESTER : REMOVE IT FROM THE MAP*/

/*WRITE ROUTE USING ENV VARIABLE*/
const route = `${process.env.REACT_APP_BASE_URL}`;

export const removeRequest = (token, requestId) => {
    return "Request has been removed"
}
